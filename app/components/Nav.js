import { Link } from "@remix-run/react";

export const Nav = () => {
  return ( 
    <div className="flex gap-5">
      {/* <Link className="bg-pink-50 hover:bg-pink-100 rounded-xl border p-3" to="/"><code>node_modules</code> import</Link> */}
    </div>
  );
}